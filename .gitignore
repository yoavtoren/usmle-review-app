node_modules
dist
dist-ios
.DS_Store
*.log
vite.config.js.timestamp-*

# First Aid copyrighted PDF — local-only, never commit/publish
public/firstaid/*.pdf
# Uncompressed original backup, kept out of the app bundle
local-assets/

# Capacitor generated / build output (regenerated by `npx cap sync ios`)
ios/App/App/public
ios/App/Pods
ios/App/build
ios/DerivedData
ios/App/App/capacitor.config.json

# Claude Code — local automation (machine-specific, not shared)
.claude/settings.local.json
.claude/.auto-sync.lock
